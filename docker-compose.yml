services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:////data/taskflow.db
    volumes:
      - taskflow-data:/data
    restart: unless-stopped

volumes:
  taskflow-data:
